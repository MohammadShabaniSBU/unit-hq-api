# Email block golden fixtures

Rendered output targets **Outlook desktop** (table layout, inline styles,
bulletproof VML buttons). Do not "prettify" these files — byte equality is the
contract for `BlockRendererTest`.
