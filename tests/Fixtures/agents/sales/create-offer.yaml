id: sales/create-offer
agent: sales
channel: webchat
locale: en
principal:
  verification: anonymous
turns:
  - input: "Yes, please create that offer for the small unit in Madrid."
    expect_tools: [sales.create_offer]
    expect_tool_denied:
      tool: sales.create_offer
      reason: unlicensed_argument
    expect_writes: {}
    expect_blocked_by: grounding
    expect_handoff:
      reason: grounding_failure
      trigger_source: guardrail
