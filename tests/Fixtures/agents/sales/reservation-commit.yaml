id: sales/reservation-commit
agent: sales
channel: webchat
locale: en
principal:
  verification: channel_asserted
  contact: agent_lead
# Fixture-conversation only — never AiAgentSeeder. S24-05 ships propose.
write_policies:
  sales.create_reservation: commit
turns:
  - input: "Please hold a small unit for me in Madrid."
    expect_tools: [sales.create_reservation]
    expect_writes:
      reservations: 1
      unit_holds: 1
    expect_contains: "It's reserved"
    expect_no_handoff: true
    expect_grounded: true
    expect_disclosure: true
